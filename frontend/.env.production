# Production environment configuration
# Empty VITE_API_BASE_URL forces apiClient to use relative '/api/v1' path,
# which proxies via vercel.json rewrites for zero CORS and seamless edge connectivity.
VITE_API_BASE_URL=
